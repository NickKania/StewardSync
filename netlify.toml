[build]
  command = "npm run build"
  publish = "dist/steward-sync/browser"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[context.production.environment]
  PUBLIC_CONVEX_URL = ""
